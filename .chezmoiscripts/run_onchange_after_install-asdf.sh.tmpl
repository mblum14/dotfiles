#!/usr/bin/env bash
# MacOS build version: {{ output "sw_vers" "--buildVersion" }}

{{ range .packages.asdf -}}
asdf plugin add {{ . }} 2>/dev/null # add plugin
asdf install {{ . }} latest         # install latest version
asdf set -u {{ . }} latest          # set as default
{{ end -}}
